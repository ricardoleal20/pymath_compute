[package]
name = "pymath_compute"
version = "0.3.1"
edition = "2021"

[dependencies]
pyo3 = { version = "0.16", features = ["extension-module"] }

[lib]
crate-type = ["cdylib"]

[package.metadata.maturin]
name = "pymath_compute.engine"
